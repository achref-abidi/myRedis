## In "./include/myredis/Server.h" : `Conn` struct
- add `write()`, `read()` functions.
- add also `safe_write()` and `safe_read()` functions.
- add `setNonBlocking()`.
- add constructor.

## In whole project
- Add a logger
- test the pipelined scenario